spring:
  application:
    name: Lab5
  datasource:
    main:
      db-name: Lab5
      driver-class-name: org.h2.Driver
      url: jdbc:h2:mem:task4;DB_CLOSE_DELAY=-1;CASE_INSENSITIVE_IDENTIFIERS=TRUE;INIT=CREATE SCHEMA IF NOT EXISTS test
      username: sa
      password: sa

db-schema: test

api-url: /app/v1